# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(camera_davisTaskLib)
ADD_LIBRARY(${CAMERA_DAVIS_TASKLIB_NAME} SHARED 
    ${CAMERA_DAVIS_TASKLIB_SOURCES})
add_dependencies(${CAMERA_DAVIS_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${CAMERA_DAVIS_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${CAMERA_DAVIS_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CAMERA_DAVIS_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CAMERA_DAVIS_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${CAMERA_DAVIS_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${CAMERA_DAVIS_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CAMERA_DAVIS_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CAMERA_DAVIS_TASKLIB_HEADERS}
    DESTINATION include/orocos/camera_davis)

